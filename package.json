{
  "name": "immigrow.ai",
  "private": true,
  "version": "0.0.0",
  "description": "Your Personal AI Immigration Assistant",
  "scripts": {
    "dev": "npm-run-all --parallel dev:frontend dev:backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w convex",
    "build": "npm run build -w frontend && npm run build -w convex",
    "lint": "npm run lint -w frontend"
  },
  "workspaces": [
    "frontend",
    "convex"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
